//Vamos lá, tutorial básico do básico, primeiros passos com C depois do aprenderem com o passo 0.

//Quando colocamos duas barras assim antes da linha, transformamos ela em um "comentário", esses são usados para aumentar a legibilidade de um código e explicar como certas coisas funcionam, basicamente o que estou fazendo agora

#include <stdio.h>  //Com essa linha, importamos o conjunto padrão de funções da linguagem C, assim poderemos usar "if" ,"else", "printf" e etc 


 //Todo código C começa a rodar a partir de uma função main, ou seja, os comandos colocados entre as chaves a seguir 
 //serão os primeiros a ser executados
int main()
{

    //Nosso primeiro comando será o famoso printf(), a partir do qual realizaremos o primeiro "Hello World"

    printf("Hello World\n");

    //Basicamente, esse comando printa na tela o conjunto de caracteres que está entre aspas duplas (Vamos ver mais coisas sobre printf daqui a pouco). O \n indica uma "quebra de linha", é tipo apertar enter 

    //Importante, depois de realizar um comando em C, coloque ;

}