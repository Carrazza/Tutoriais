#include <stdio.h>

int main()
{
    //agora que vcs sabem um dos comandos mais essenciais de C, hora de aprender sobre variáveis. 
    //variáveis são pequenos espaços alocados na memoria física do seu computador em que vc guarda informações essenciais como números, frases etc 

    //temos vários tipos de variáveis, tem uma foto com mais informações sobre elas na pasta 

    int inteiro = 25; //aqui declaramos nossa primeira variável do tipo inteiro, ela suporta números sem casas decimais

    float decimal = 12.5; //uma do tipo float que aguenta os decimais 

    char letrinha = 'a';  //char guarda letrinhas (apenas uma letra, vou deixar a explicação de como guardar frase com o Thiago em IP)

    //uma vez declarada, n precisa mais colocar o int,float,char. O lugar na memória da variável já está definido 

    inteiro = 22; // mudando o valor da variável  

    //Oq se pode fazer com variáveis

    int a = 10; 

    int b = 15; 

    int c = a+b; // c= 25

    c = a - b; // c = -5 

    c = a/b; // 




}